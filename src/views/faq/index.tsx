import { FAQPage } from "features/faq";

export const FAQ = () => {
  return (
    <div
      className="w-100"
      style={{
        marginTop: "100px",
      }}
    >
      <FAQPage />
    </div>
  );
};
